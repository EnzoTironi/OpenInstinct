# Names only — never commit real secret values.
COMPANION_POSTGRES_PASSWORD=postgres

# Option C (Alchemy unmanaged Fly Postgres) — optional overrides:
# FLY_API_TOKEN=          # or: alchemy login / fly auth token
# COMPANION_FLY_PG_REGION=gru
# COMPANION_FLY_PG_VOLUME_GB=10
# COMPANION_FLY_PG_APP_NAME=companion-pg-prod
